<Views:ViewBase x:Class="Amuse.App.Views.SettingsEnvironmentView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:local="clr-namespace:Amuse.App.Views"
    xmlns:CommonControls="clr-namespace:TensorStack.WPF.Controls;assembly=TensorStack.WPF"
    xmlns:Controls="clr-namespace:Amuse.App.Controls"
    xmlns:Views="clr-namespace:Amuse.App.Views">
    <GroupBox DataContext="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:SettingsEnvironmentView}}}" Margin="10, 20, 20, 20" Header="{CommonControls:Localize EnvironmentSettings}" Padding="4">
        <Grid >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>

            <!--Environments-->
            <DockPanel Grid.Column="0" >

                <DockPanel DockPanel.Dock="Top" Margin="0,0,0,6">
                    <Border DockPanel.Dock="Left" Width="26" BorderThickness="1" BorderBrush="{StaticResource DefaultBorderColor}">
                        <CommonControls:FontAwesome Icon="f002" Size="12"/>
                    </Border>
                    <CommonControls:IconButton DockPanel.Dock="Right" Icon="f00d" IconSize="12" IconColor="{StaticResource DangerColour}" Width="23" Command="{Binding FilterClearCommand}"/>
                    <CommonControls:PlaceholderTextBox Text="{Binding FilterText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Placeholder="Search Environments" PlaceholderOpacity=".5"/>
                </DockPanel>

                <StackPanel DockPanel.Dock="Bottom">
                    <UniformGrid  Columns="3" Height="50">
                        <CommonControls:IconButton Text="{CommonControls:Localize Add}" FontSize="13" Icon="2b" IconStyle="Light" IconColor="{StaticResource SuccessColour}" Command="{Binding AddEnvironmentCommand}"  />
                        <CommonControls:IconButton Text="{CommonControls:Localize Copy}" FontSize="13" Icon="f24d" IconStyle="Light" IconColor="{StaticResource InfoColour}" Command="{Binding CopyEnvironmentCommand}"  />
                        <CommonControls:IconButton Text="{CommonControls:Localize Edit}" FontSize="13" Icon="f0ad" IconStyle="Light" IconColor="{StaticResource InfoColour}" Command="{Binding UpdateEnvironmentCommand}"  />
                        <CommonControls:IconButton Text="{CommonControls:Localize Import}" FontSize="13" Icon="f56f" IconStyle="Light" IconColor="{StaticResource InfoColour}" Command="{Binding ImportEnvironmentCommand}"  />
                        <CommonControls:IconButton Text="{CommonControls:Localize Export}" FontSize="13" Icon="f56e" IconStyle="Light" IconColor="{StaticResource InfoColour}" Command="{Binding ExportEnvironmentCommand}"  />
                        <CommonControls:IconButton Text="{CommonControls:Localize Remove}" FontSize="13" Icon="f00d" IconStyle="Light" IconColor="{StaticResource DangerColour}" Command="{Binding RemoveEnvironmentCommand}"  />
                    </UniformGrid>
                </StackPanel>

                <ListBox ItemsSource="{Binding ModelCollection}" SelectedItem="{Binding SelectedEnvironment}" ItemContainerStyle="{StaticResource SettingsListBoxItem}" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <DockPanel Margin="2">
                                <StackPanel DockPanel.Dock="Left" Margin="3,0,0,0">
                                    <TextBlock Text="{Binding Name}" FontSize="12" FontWeight="Medium" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding Pipeline, TargetNullValue=All Pipelines}" Opacity=".7" FontSize="11"/>
                                </StackPanel>
                                <StackPanel>
                                    <TextBlock Text="{Binding Type}" TextAlignment="Right" Opacity=".7" FontSize="11"/>
                                    <TextBlock Text="{Binding Vendor}" TextAlignment="Right" Opacity=".7" FontSize="11"/>
                                </StackPanel>
                            </DockPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>

            <DockPanel Grid.Column="1"  Margin="10, 0,0,0">
                <DockPanel DockPanel.Dock="Top">

                    <!--Actions-->
                    <DockPanel DockPanel.Dock="Right" Width="174">
                        <TextBlock DockPanel.Dock="Top" Text="{CommonControls:Localize Actions}" Style="{StaticResource FieldTextBlockStyle}" />
                        <UniformGrid  Columns="2" VerticalAlignment="Top"  Visibility="{Binding SelectedEnvironment, Converter={StaticResource NullVisibilityConverter}}" >
                            <CommonControls:IconButton Text="{CommonControls:Localize Create}" FontSize="13" Icon="f0ed" IconSize="14" IconStyle="Light" IconOpacity=".8" IconColor="{StaticResource InfoColour}" Command="{Binding EnvironmentServiceCreateCommand}" Height="40" Alignment="Left" IconMargin="11,0,1,0" />
                            <CommonControls:IconButton Text="{CommonControls:Localize Delete}" FontSize="13" Icon="f2ed" IconSize="14" IconStyle="Light" IconOpacity=".8" IconColor="{StaticResource DangerColour}" Command="{Binding EnvironmentServiceDeleteCommand}" Height="40" Alignment="Left" IconMargin="13,0,1,0" />
                            <CommonControls:IconButton Text="{CommonControls:Localize Update}" FontSize="13" Icon="f01e" IconSize="14" IconStyle="Light" IconOpacity=".8" IconColor="{StaticResource WarningColour}" Command="{Binding EnvironmentServiceUpdateCommand}" Height="40" Alignment="Left" IconMargin="10,0,1,0" />
                            <CommonControls:IconButton Text="{CommonControls:Localize Rebuild}" FontSize="13" Icon="f361" IconSize="14" IconStyle="Light" IconOpacity=".8" IconColor="{StaticResource WarningColour}" Command="{Binding EnvironmentServiceRebuildCommand}" Height="40" Alignment="Left" IconMargin="9,0,1,0" />
                        </UniformGrid>
                    </DockPanel>

                    <!--Environment-->
                    <DockPanel Margin="0,0,100,0">
                        <StackPanel DockPanel.Dock="Top">
                            <TextBlock Text="{CommonControls:Localize Name}" Style="{StaticResource FieldTextBlockStyle}" />
                            <TextBox Text="{Binding SelectedEnvironment.Name}" IsReadOnly="True"/>
                        </StackPanel>

                        <DockPanel DockPanel.Dock="Bottom" Margin="0,10,4,0">
                            <Grid DockPanel.Dock="Left"  Height="28" Width="200">
                                <Button Command="{Binding EnvironmentDefaultCommand}" >
                                    <TextBlock Text="{CommonControls:Localize EnvironmentSetDefault}"/>
                                </Button>
                                <Button IsEnabled="False" Visibility="{Binding SelectedEnvironment.IsDefault, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <DockPanel>
                                        <CommonControls:FontAwesome DockPanel.Dock="Left" Icon="f00c" Size="13" Color="{StaticResource SuccessColour}" Margin="0,0,5,0"/>
                                        <TextBlock Text="{CommonControls:Localize EnvironmentDefault}" Foreground="{StaticResource SuccessColour}"/>
                                    </DockPanel>
                                </Button>
                            </Grid>
                            <Grid></Grid>
                        </DockPanel>

                        <UniformGrid Rows="1">
                            <StackPanel Margin="0,0,2,0">
                                <TextBlock Text="{CommonControls:Localize Type}" Style="{StaticResource FieldTextBlockStyle}" />
                                <TextBox Text="{Binding SelectedEnvironment.Type}" IsReadOnly="True"/>
                            </StackPanel>
                            <StackPanel Margin="2,0,2,0">
                                <TextBlock Text="{CommonControls:Localize Vendor}" Style="{StaticResource FieldTextBlockStyle}" />
                                <TextBox Text="{Binding SelectedEnvironment.Vendor}" IsReadOnly="True"/>
                            </StackPanel>
                            <Grid Margin="2,0,0,0" IsHitTestVisible="False" Visibility="{Binding  SelectedEnvironment.Type, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=Device|Pipeline}">
                                <StackPanel Visibility="{Binding  SelectedEnvironment.Type, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=Device}">
                                    <TextBlock Text="{CommonControls:Localize Device}" Style="{StaticResource FieldTextBlockStyle}"/>
                                    <ComboBox ItemsSource="{Binding Settings.Devices}" SelectedValue="{Binding SelectedEnvironment.Device}" DisplayMemberPath="Name" SelectedValuePath="HardwareID"/>
                                </StackPanel>
                                <StackPanel Visibility="{Binding SelectedEnvironment.Type, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=Pipeline}">
                                    <TextBlock Text="{CommonControls:Localize Pipeline}" Style="{StaticResource FieldTextBlockStyle}"/>
                                    <TextBox Text="{Binding SelectedEnvironment.Pipeline}" IsReadOnly="True" />
                                </StackPanel>
                            </Grid>
                            <StackPanel Margin="2,0,2,0">
                                <TextBlock Text="{CommonControls:Localize Environment}" Style="{StaticResource FieldTextBlockStyle}" />
                                <TextBox Text="{Binding SelectedEnvironment.Environment}" IsReadOnly="True"/>
                            </StackPanel>
                            <StackPanel Margin="2,0,2,0">
                                <TextBlock Text="{CommonControls:Localize Version}" Style="{StaticResource FieldTextBlockStyle}" />
                                <TextBox Text="{Binding SelectedEnvironment.HostVersion}" IsReadOnly="True"/>
                            </StackPanel>
                        </UniformGrid>

                    </DockPanel>


                </DockPanel>
                <StackPanel DockPanel.Dock="Bottom">
                    <TextBlock Text="{CommonControls:Localize EnvironmentVariables}" Style="{StaticResource FieldTextBlockStyle}" />
                    <ListBox ItemsSource="{Binding SelectedEnvironment.Variables}"  ScrollViewer.HorizontalScrollBarVisibility="Disabled" MinHeight="100"/>
                </StackPanel>
                <DockPanel>
                    <TextBlock  DockPanel.Dock="Top" Text="{CommonControls:Localize Requirements}" Style="{StaticResource FieldTextBlockStyle}" />
                    <ListBox ItemsSource="{Binding SelectedEnvironment.Requirements}"  ScrollViewer.HorizontalScrollBarVisibility="Disabled"/>
                </DockPanel>
            </DockPanel>

        </Grid>
    </GroupBox>
</Views:ViewBase>
